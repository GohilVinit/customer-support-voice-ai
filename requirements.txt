# Core Flask and web framework
Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0

# OpenAI for Whisper transcription
openai>=1.3.7

# Google Generative AI for Gemini chat/completion and embeddings
google-generativeai==0.3.2

# Audio/Video processing
moviepy==1.0.3
pydub==0.25.1

# LangChain for RAG framework
langchain==0.0.350
langchain-community==0.0.10

# Vector store and embeddings (Python 3.11 compatible)
# Use ChromaDB as primary vector store (works better on Windows)
chromadb==0.4.18
# Alternative: Use sentence-transformers for embeddings
sentence-transformers==2.2.2
# FAISS is optional - uncomment if you can install it successfully
# faiss-cpu==1.7.4

# Memory management and system monitoring
psutil==5.9.6

# Google Drive integration
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# HTTP requests and utilities
requests==2.31.0

# Data processing (Python 3.12.2 compatible versions)
numpy==1.26.2
pandas==2.1.4

# File handling and utilities
Werkzeug==2.3.7

# Logging and debugging
colorama==0.4.6

# Streamlit for frontend
streamlit==1.28.1

# PDF processing for frontend
fpdf==1.7.2

# Enhanced analysis dependencies (Python 3.12.2 compatible)
scikit-learn==1.3.2
scipy==1.11.4
librosa==0.10.1
soundfile==0.12.1
jsonschema==4.20.0

# Setuptools for compatibility (required for Python 3.12.2)
setuptools>=68.0.0
wheel>=0.41.0

# Note: pyannote.audio is NOT compatible with Python 3.12.2
# This feature is disabled for Python 3.12.2 users
# To enable speaker diarization, use Python 3.11
# pyannote.audio==3.1.1 